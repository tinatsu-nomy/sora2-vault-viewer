# Sora2 Vault Viewer

A lightweight local viewer for browsing Sora manifest exports together with local `mp4` and `txt` files.

This project assumes the data structure produced by [SoraVault 2.0](https://github.com/charyou/SoraVault), including `soravault_manifest_*.json` files and the `sora_v2_profile`, `sora_v2_liked`, and `sora_v2_drafts` directory layout.
This repository does not include or reuse SoraVault source code. It only works with the data structure generated by SoraVault 2.0 exports.

## Features

- Merges all `soravault_manifest_*.json` files found in `sora2_data/`
- Matches manifest items with local files from:
  - `sora_v2_profile`
  - `sora_v2_liked`
  - `sora_v2_drafts`
- Plays local videos inline in a dark gallery UI
- Searches by prompt, `genId`, `postId`, `taskId`, and TXT content
- Filters by source, local video, and TXT availability
- Shows prompt, TXT, local file paths, external links, and manifest JSON details
- Includes optional SQLite cache support when available

## Requirements

- Node.js 22.13+

## Project Structure

```text
app/
  public/
    index.html
    app.js
    styles.css
  server.js
sora2_data/
  soravault_manifest_*.json
  sora_v2_profile/
  sora_v2_liked/
  sora_v2_drafts/
```

## Run

```powershell
npm start
```

Then open the local URL shown in the terminal. The app starts at `http://localhost:3210` by default and automatically moves to the next free port if needed.

To stop the local server, return to the terminal where it is running and press `Ctrl+C`.

## Configuration

The app works out of the box when the repository contains a `sora2_data/` directory.

Optional environment variables:

- `PORT`: starting port for the local server
- `SORA_VIEWER_ROOT`: override the repository root
- `SORA_DATA_DIR`: override the data directory location
- `SORA_SQLITE_PATH`: override the SQLite cache file path

## Adding Data

### Add new manifest JSON files

1. Copy the new `soravault_manifest_*.json` file into `sora2_data/`
2. Keep the `soravault_manifest_*.json` naming pattern so the viewer can detect it automatically
3. Restart the server or click `Rescan` in the viewer
4. Confirm the file name appears in the `Loaded manifests` section in the summary area

### Add new local media and TXT files

1. Put exported files into the matching source directory:
   - `sora_v2_profile`
   - `sora_v2_liked`
   - `sora_v2_drafts`
2. Keep each `mp4` and `txt` pair together using the exported file names from SoraVault 2.0
3. Restart the server or click `Rescan` in the viewer
4. Confirm the new items appear in the gallery and detail view

## Notes for GitHub

- Local media, manifests, caches, and backup logs are intentionally excluded by `.gitignore`
- External `preview` / `download` / `thumb` URLs are shown as links only and are not fetched automatically by the viewer
- Verified in practice with Node.js 24
- This repository is provided as-is, without warranty or guarantee of compatibility, correctness, completeness, or fitness for a particular purpose
- Behavior is only intended for data layouts captured through SoraVault 2.0 and is not guaranteed for other export formats or future versions
- No SoraVault source code is bundled or reused here; only its exported data structure is referenced
- Sora 1 data is not supported at this time because no Sora 1 export data was available for development or validation

## License

MIT
